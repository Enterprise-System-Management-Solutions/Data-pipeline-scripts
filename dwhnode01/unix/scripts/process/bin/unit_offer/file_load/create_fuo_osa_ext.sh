PATH=$PATH:$HOME/.local/bin:$HOME/bin

export PATH

export TMP=/tmp
export TMPDIR=$TMP

export ORACLE_HOSTNAME=dwhnode01
export ORACLE_UNQNAME=dwhdb01
export ORACLE_BASE=/data01/app/oracle
export ORACLE_HOME=$ORACLE_BASE/product/19.0.0/dbhome_1
export ORA_INVENTORY=/data01/app/oraInventory
export ORACLE_SID=dwhdb01
export DATA_DIR=/data01/oradata

export PATH=/usr/sbin:/usr/local/bin:$PATH
export PATH=$ORACLE_HOME/bin:$PATH

export LD_LIBRARY_PATH=$ORACLE_HOME/lib:/lib:/usr/lib
export CLASSPATH=$ORACLE_HOME/jlib:$ORACLE_HOME/rdbms/jlib


FILE_NAME="FUO_OCS_SUBSCRIBER_ALL_EXT"  export FILE_NAME

##echo "-------------------------------- $1 --------------------------------"
echo "Create table started at `date \"+%D %T\"`"


sqlplus -s <<EOF
dwh_user/dwh_user_123
set feedback off
drop table $FILE_NAME
/
create table $FILE_NAME
(
  MSISDN             VARCHAR2(256 BYTE),
  SUBCOSID           VARCHAR2(256 BYTE),
  BALANCE            VARCHAR2(256 BYTE),
  ACTIVEFLAG         VARCHAR2(256 BYTE),
  TIMEENTERACTIVE    VARCHAR2(256 BYTE),
  ACTIVESTOP         VARCHAR2(256 BYTE),
  SUSPENDSTOP        VARCHAR2(256 BYTE),
  DISABLESTOP        VARCHAR2(256 BYTE),
  USERTYPE           VARCHAR2(256 BYTE),
  CUSTOMERTYPE       VARCHAR2(256 BYTE),
  CUSTOMERLEVEL      VARCHAR2(256 BYTE),
  LASTACTIVEDATE     VARCHAR2(256 BYTE),
  USERSTATE          VARCHAR2(256 BYTE),
  DEREGISTERFLAG     VARCHAR2(256 BYTE),
  LOSSFLAG           VARCHAR2(256 BYTE),
  FRAUDFLAG          VARCHAR2(256 BYTE),
  APPLYTIME          VARCHAR2(256 BYTE),
  EXPIRETIME         VARCHAR2(256 BYTE),
  LASTCALLTIME       VARCHAR2(256 BYTE),
  LOCATION           VARCHAR2(256 BYTE),
  IMSI               VARCHAR2(256 BYTE),
  SIM                VARCHAR2(256 BYTE),
  IMEI               VARCHAR2(256 BYTE),
  SERVICETYPE        VARCHAR2(256 BYTE),
  STOPFLAG           VARCHAR2(256 BYTE),
  KEEPFLAG           VARCHAR2(256 BYTE),
  BLOCKFLAG          VARCHAR2(256 BYTE),
  CURRENTSTATE       VARCHAR2(256 BYTE),
  SUBSCRIBERTYPE     VARCHAR2(256 BYTE),
  NETWORKTYPE        VARCHAR2(256 BYTE),
  CREDITLEVEL        VARCHAR2(256 BYTE),
  SUBSCRIBERCLASS    VARCHAR2(256 BYTE),
  CREATEFROM         VARCHAR2(256 BYTE),
  LOCALAREA          VARCHAR2(256 BYTE),
  HOMECBP            VARCHAR2(256 BYTE),
  SERVICEORDERFLAG   VARCHAR2(256 BYTE),
  SRVREQSUSPENDFLAG  VARCHAR2(256 BYTE),
  SUBSCRIBERKEY      VARCHAR2(256 BYTE),
  BRANDID            VARCHAR2(256 BYTE),
  SUBSCRIBERCODE     VARCHAR2(256 BYTE),
  OWNCUSTOMER        VARCHAR2(256 BYTE),
  ACCOUNTCREDIT      VARCHAR2(256 BYTE),
  ACCOUNTKEY         VARCHAR2(256 BYTE)
)
ORGANIZATION EXTERNAL
  (  TYPE ORACLE_LOADER
     DEFAULT DIRECTORY FUO_OSA_DIR
     ACCESS PARAMETERS 
       ( RECORDS DELIMITED by NEWLINE
        BADFILE     '$1.bad'
        DISCARDFILE '$1.dis'
        FIELDS TERMINATED BY '|'
  )
     LOCATION (FUO_OSA_DIR:'$1.list')
  )
REJECT LIMIT UNLIMITED
NOPARALLEL
NOMONITORING;
exit
EOF

echo "External table created"

